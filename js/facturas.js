const listafactura = [];

const agregarProducto = () => {

    const fechaf = document.getElementById('fechaf');
    const nombresel = document.getElementById('nombreselect');
    const productosel = document.getElementById('productselect');
    const cantsel = document.getElementById('cantidadf');

    console.log(fechaf.value)
    console.log(nombresel.value)
    console.log(productosel.value)
    console.log(cantsel.value)

    /*   for(let i=0;i<=10;i++){
           const nuevoProducto={
               id:"PROD"+i,
               descripcion:faker.commerce.productName(),
               precio: Math.floor(Math.random() * (150 - 0) + 0)
               
           };
           listaProductos.push(nuevoProducto);
       }    
   */
}
